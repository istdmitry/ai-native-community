{
  "name": "ai-native-community",
  "version": "0.1.0",
  "private": true,
  "description": "AI-Native Community platform",
  "scripts": {
    "dev": "pnpm --filter @community/web dev",
    "dev:mcp": "pnpm --filter @community/mcp dev",
    "build": "pnpm -r build",
    "type-check": "pnpm -r type-check",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.19.33"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20.0.0"
  }
}
