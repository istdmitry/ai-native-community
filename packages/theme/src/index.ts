/**
 * @community/theme — Single dark theme for AI-Native Community
 *
 * Maps org design system tokens (style1-dark.json) to CSS variables.
 * No multi-skin, no partner resolution — just one dark theme.
 */

export { ThemeProvider } from './ThemeProvider';
export { tokens } from './tokens';
export type { CommunityTokens } from './tokens';
